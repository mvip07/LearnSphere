import React from "react";

export const metadata = {
  title: "Top Ranking Users - Leaderboard",
  description: "Explore the highest-ranking users based on performance, achievements, and quiz scores in the leaderboard.",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children
} 