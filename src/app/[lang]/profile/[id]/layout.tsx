export const metadata = {
    title: "User Profile",
    description: "View detailed public profile information, achievements, and activity of a user.",
};

export default function ProfileLayout({ children }: { children: React.ReactNode }) {
    return children;
}