export const metadata = {
  title: "User Detail | Admin Panel",
  description: "View detailed information about a specific user, including profile, activity, and quiz performance.",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children;
}