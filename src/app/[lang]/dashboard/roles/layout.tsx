export const metadata = {
    title: "Roles | Admin Panel",
    description: "Define and manage user roles to assign responsibilities and control access within the admin panel.",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
    return children;
}