export const metadata = {
    title: "Levels | Admin Panel",
    description: "Create, edit, and manage quiz difficulty levels to customize challenges for different user skills.",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
    return children;
}