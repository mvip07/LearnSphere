export const metadata = {
    title: "Permissions | Admin Panel",
    description: "Control and assign access rights to users, manage system permissions, and ensure secure operations.",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
    return children;
}