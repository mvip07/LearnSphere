export const metadata = {
    title: "Questions | Admin Panel",
    description: "Add, edit, and manage quiz questions including text, image, video, and audio formats for interactive learning.",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
    return children;
}